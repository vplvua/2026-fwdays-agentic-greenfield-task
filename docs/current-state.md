# Current State (2026-07-08, день)

> Persistent memory bank: відбиток поточного стану, **не лог**. Оновлюється
> наприкінці кожної сесії/зрізу (DoD п.8). Формат фіксований: Phase / Done last
> session / Next 1-2 tasks / Blockers.

## Phase

- S-03 «Довідник будинків» завершено (перше доменне CRUD з ізоляцією
  власника — патерн для всіх наступних зрізів); далі — S-04 «Заявка:
  створення, редагування, картка»

## Done last session

- S-03 повністю: Prisma-модель `house` (+міграція); CRUD
  `/api/houses` за глобальним SessionGuard; власницька перевірка одним
  запитом `id AND user_id` → однакове 404-тіло для чужого й неіснуючого
  (FR-ACCESS-01/NFR-SEC-03, канон для S-04+); валідація в сервісі
  (назва ≤255, примітка ≤1000), locale-free коди `HouseError`
- Web: екран «Будинки» (`features/houses`: facade на сигналах з
  reload-after-mutation, house-list/house-card презентаційні, діалоги
  створення/редагування і підтвердження видалення, снекбар для помилок);
  навігація з домашньої; `guestGuard` — авторизований на /login → додому
- Тести: api unit 75, web unit 31, api-e2e 24, Playwright S-03 (3 сценарії
  ×3 браузери) + повторний прогін S-02; смок на реальній MySQL пройдено
- Ревʼю (ADR-0010): APPROVE з першого разу (діапазон 9b9da69..9d6efde);
  1 medium (trim імені в діалозі) виправлено + e2e, 1 low (апостроф)
  виправлено, 1 low (гонка update-vs-delete → 404) прийнято — диспозиція
  в ретро
- Launch-and-look виявив: іконковий шрифт Material Icons ніколи не
  підключався (перше використання mat-icon у проєкті) — додано в
  index.html. Fallow: 2 false positive (input у `@else if`,
  template-складність контейнера) — обійдено реструктуризацією
  і документованим suppression
- FR-HOUSE-02 (заборона видалення з заявками): контракт у спеці, тест
  відмови відкладено до S-04 (FK `onDelete: Restrict` + сервісна
  перевірка — design D3 архівованого change)

## Next 1-2 tasks

- [ ] `/slice-retro` S-03 → `docs/cycles/S-03.md` (метрики /cost від
      користувача, диспозиції ревʼю)
- [ ] `/opsx:propose` S-04 «Заявка: створення, редагування, картка»
      (FR-TICKET-01/02/04; enum-и §5.2–5.3; **не забути** FK
      house→ticket з перевіркою FR-HOUSE-02 і тестом відмови видалення)

## Blockers

- (none). Прод-деплой S-03 не виконувався в сесії — зріз deployable,
  деплой Railway за потреби (`git push` тригерить збірку). Локальна
  особливість: порт 3000 періодично зайнятий іншим проєктом
  (`~/Projects/cabinet`)
- ⚠ Робоча гілка — **`service-desk-mini`** (ADR-0012): `main` дзеркалить
  стартер курсу, проєктні коміти в нього не йдуть
