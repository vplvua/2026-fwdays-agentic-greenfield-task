# Current State (2026-07-08, ніч)

> Persistent memory bank: відбиток поточного стану, **не лог**. Оновлюється
> наприкінці кожної сесії/зрізу (DoD п.8). Формат фіксований: Phase / Done last
> session / Next 1-2 tasks / Blockers.

## Phase

- S-05 «Життєвий цикл і стрічка» завершено (повний 5-статусний цикл §5.1 +
  єдина append-only стрічка); далі — S-06 «Список, фільтри, пошук,
  прострочення»

## Done last session

- S-05 повністю: таблиця `ticket_feed_item` (NOTE/EVENT, локале-незалежні
  снапшоти значень, append-only, міграція); `POST /api/tickets/:id/transition`
  — таблиця §5.1 у сервісі (`ALLOWED_TRANSITIONS`), статус-гвардований
  `updateMany` + подія STATUS в одній транзакції, stale-запит → 409
  `TICKET_TRANSITION_FORBIDDEN`; `GET :id/feed`, `POST :id/notes`
  (404-парність FR-ACCESS-01); PATCH пише події FR-TICKET-03 (5 полів,
  снапшоти назв будинку, set/clear терміну) атомарно з апдейтом; картка
  віддає `allowedTransitions` — SPA не володіє правилами переходів
- Web: кнопки переходів (назви дій §5.1), стрічка з візуально різними
  записами/подіями (автор + час), форма запису; фасад: reload-after-mutation,
  свіжий ticket комітиться ДО перечитування стрічки (рев'ю-фікс)
- Тести: api unit 139 (+31), web unit 59 (+9), api-e2e 49 (+13), Playwright
  72 (S-05: 4 сценарії ×3 браузери + регрес S-01…S-04); смок на реальній
  MySQL 19/19; launch-and-look 390×844 пройдено очима
- Знахідка e2e: `(ngSubmit)` без form-директиви не стріляє — нативний submit
  перезавантажив сторінку; виправлено на `FormGroup` (Playwright зловив)
- Ревʼю (ADR-0010): APPROVE (діапазон 73caac0..f3824e5) — 1 medium прийнято
  (stale-статус при падінні перечитування стрічки після успішної мутації →
  фікс + регрес-тест), 1 low виправлено (загублені чекбокси tasks.md —
  маркування було в одній Bash-команді із заблокованим комітом); пропозицію
  окремої копії «запис міг зберегтись» відхилено на POC-масштабі
- Fallow двічі блокував коміт на складності нових функцій
  (`diffTrackedFields`, `eventValueLabel`) — обидві спрощено табличним
  підходом замість suppression

## Next 1-2 tasks

- [ ] `/opsx:propose` S-06 «Список, фільтри, пошук, прострочення»
      (FR-LIST-01…04, FR-DUE-02, NFR-PERF-01, NFR-COMPAT-01; індекси
      `user_id+status`, `user_id+house_id`; LIKE-пошук; пресет «активні»;
      підсвічування прострочених у списку і картці)
- [ ] Прод-деплой S-04+S-05 за потреби (`git push` тригерить Railway)

## Blockers

- (none)
- ⚠ Робоча гілка — **`service-desk-mini`** (ADR-0012): `main` дзеркалить
  стартер курсу, проєктні коміти в нього не йдуть
