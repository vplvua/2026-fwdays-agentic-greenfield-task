# Current State (2026-07-08, ніч)

> Persistent memory bank: відбиток поточного стану, **не лог**. Оновлюється
> наприкінці кожної сесії/зрізу (DoD п.8). Формат фіксований: Phase / Done last
> session / Next 1-2 tasks / Blockers.

## Phase

- S-06 «Список, фільтри, пошук, прострочення» завершено (список з
  комбінованими фільтрами, LIKE-пошуком, сортуванням, пагінацією і
  підсвічуванням прострочених); далі — S-07 «Фото-вкладення»

## Done last session

- S-06 повністю: `GET /api/tickets` — owner-scoped список з AND-фільтрами
  (статус/`ACTIVE`-пресет розгортається на сервері, будинок, категорія,
  пріоритет), LIKE-пошук по 5 полях (вайлдкарди екрануються — пошук
  літеральний), сортування createdAt/dueDate (nulls last, tie-break id
  desc), пагінація `{ items, total, page, pageSize }`; помилкові
  query-параметри → 400 `TICKET_QUERY_INVALID`
- `isOverdue` рахує сервер (§5.4, «сьогодні» в Europe/Kyiv) — спільна
  константа `ACTIVE_STATUSES` живить і пресет, і прапорець; картка отримала
  той самий прапорець (бейдж «Прострочено»)
- Індекси `user_id+status`, `user_id+house_id`; надлишковий `user_id`
  знято окремою ручною міграцією (Prisma не дропає імпліцитний FK-індекс —
  довелось писати SQL руками, дрейфу нема)
- Web: `TicketListPage` на `/tickets` — URL є єдиним джерелом стану
  фільтрів/пошуку/сортування (deep-link, reload, back); глибина
  «Показати ще» свідомо в памʼяті (перезавантаження стартує зі стор. 1 —
  spec/design поправлені під це під час apply); чіпи статусів, селекти,
  дебаунс-пошук 300 мс; головна отримала лінк «Заявки»
- Тести: api unit 172 (+33), web unit 80 (+21), api-e2e 62 (+13),
  Playwright 87 (S-06: 5 сценаріїв ×3 браузери + повний регрес); смок на
  реальній MySQL — 300 заявок, 12 інваріантів, p95 списку 16 мс
  (бюджет NFR-PERF-01 — 1000 мс); launch-and-look 390×844 очима:
  список/пресет/прострочення/картка/«Показати ще» — ок
- Ревʼю (ADR-0010): PASS_WITH_NOTES (діапазон 291877e..0cd19ae) — 1 medium
  виправлено (невдале «Показати ще» ховало вже показаний список за
  повноекранною помилкою → окремий канал `listMoreError` + снекбар, список
  не демонтується), 2 low виправлено (екранування LIKE-вайлдкардів —
  перевірено api-e2e на реальній MySQL; design D4 називав не ту колацію)
- Заархівовано `ticket-list-filters`; специфікації синхронізовано вручну
  (`ticket-list` нова + дельта `ticket-crud`), `npx openspec list` порожній

## Next 1-2 tasks

- [ ] `/opsx:propose` S-07 «Фото-вкладення» (FR-ATTACH-01…03, NFR-STOR-01;
      тут закривається В-02 HEIC — рішення фіксується Р-записом; файли на
      Railway Volume, роздача лише через API з перевіркою власника)
- [ ] Прод-деплой S-04…S-06 за потреби (`git push` тригерить Railway)

## Blockers

- (none)
- ⚠ Робоча гілка — **`service-desk-mini`** (ADR-0012): `main` дзеркалить
  стартер курсу, проєктні коміти в нього не йдуть
