# Current State (2026-07-09)

> Persistent memory bank: відбиток поточного стану, **не лог**. Оновлюється
> наприкінці кожної сесії/зрізу (DoD п.8). Формат фіксований: Phase / Done last
> session / Next 1-2 tasks / Blockers.

## Phase

- **POC закрито**: S-08 «Поліровка, наскрізна перевірка, ретроспектива POC»
  завершено — всі зрізи S-01…S-08 виконані, traceability 100%, продуктовий
  критерій PRD §3.2 підтверджено на проді. Лишилась фінальна здача:
  PR `service-desk-mini` → `main` форку (ADR-0008/0012)

## Done last session

- NFR-OBS-01 закрито повністю: capability `structured-logging` — JSON-логи
  в проді (`JsonConsoleLogger`, поля запиту в топ-рівні конверта, бо
  Railway «губить» обʼєктний `message`), request-middleware для `/api/*`
  (метод/шлях без query string/статус/тривалість; compact-рядок у dev),
  401/404 видимі (middleware, не інтерсептор), стек 500-ї через штатний
  exceptions handler; PII-аудит усіх call sites — чисто
- Наскрізний прохід §6 на проді реальним користувачем з реальним SMS:
  вхід → заявка з фото → коментарі/редагування → фільтри → закриття — без
  зауважень (список UX-правок порожній); «фото живе після редеплою»
  (NFR-STOR-01) підтверджено — останній відкладений demo-чек закрито
- Ревʼю (ADR-0010): PASS_WITH_NOTES (864c787..92f8055), 3 medium + 2 low;
  виправлено: захист конверта від колізій payload-ключів, лог успішного
  відправлення TurboSMS (маскований телефон) + сценарій у спеці, точний
  матч сегмента `/api`, спека приведена до фактичного винятку dev-сендера
  (повний телефон+код лише в dev, S-02); прийнято як POC-трейдоф: аборт
  зʼєднання без 'finish' не логується (диспозиція в ретро)
- `docs/poc-report.md` v1.1: метрики S-01…S-07 ($478.98, ≈7.5 год API,
  ≈20 хв людини), якість, висновки для основного проєкту; фіналізується
  цифрами S-08 після ретро
- `docs/traceability-matrix.md` v1.8 — 100%, жодної непокритої клітинки
- Тести: api unit 216 (+11), web unit 96, api-e2e 73, Playwright 96/96
  (один флейк S-02-спеки на першому прогоні, зник на повторі); прод-смок
  логів локально + `railway logs` очима
- Заархівовано `poc-polish-observability`; spec синхронізовано в
  `openspec/specs/structured-logging/`, `npx openspec list` порожній

## Next 1-2 tasks

- [ ] Ретро S-08 (`/slice-retro` → `docs/cycles/S-08.md`) + фінальні цифри
      S-08 у `docs/poc-report.md` (якщо ще не зроблено цієї сесії)
- [ ] Фінальна здача курсу: PR `service-desk-mini` → `main` форку
      (`.github/pull_request_template.md`, ревʼю CodeRabbit) — єдиний PR
      проєкту (ADR-0008/0012)

## Blockers

- (none)
- ⚠ Робоча гілка — **`service-desk-mini`** (ADR-0012): `main` дзеркалить
  стартер курсу, проєктні коміти в нього не йдуть
- ⚠ Прод-деплой = `railway up` з робочої копії (GitHub-інтеграції немає —
  рішення В-06: лишаємо CLI-флоу)
