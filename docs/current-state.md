# Current State (2026-07-08, вечір)

> Persistent memory bank: відбиток поточного стану, **не лог**. Оновлюється
> наприкінці кожної сесії/зрізу (DoD п.8). Формат фіксований: Phase / Done last
> session / Next 1-2 tasks / Blockers.

## Phase

- S-04 «Заявка: створення, редагування, картка» завершено (ядро домену:
  `ticket` + перші мобільні-пріоритет екрани); далі — S-05 «Життєвий цикл
  і стрічка»

## Done last session

- S-04 повністю: Prisma-модель `ticket` (+enum-и категорій/пріоритетів/
  статусів §5.1–5.3, міграція); `POST/GET/PATCH /api/tickets` за
  SessionGuard; власницька перевірка S-03-ідіомою для заявки **і** для
  `houseId` з тіла (404-парність, FR-ACCESS-01); статус не пишеться через
  create/PATCH (переходи — S-05); DELETE/list відсутні (FR-TICKET-04/S-06);
  номер #N = BIGINT AI id
- FR-HOUSE-02 закрито (борг S-03): FK `Restrict` + count-перевірка в
  видаленні будинку → `HOUSE_HAS_TICKETS` (409), P2003 як гоночний бекстоп;
  e2e-тест відмови
- Web: `features/tickets` (facade на сигналах, презентаційні ticket-form/
  ticket-card, контейнери /tickets/new|:id|:id/edit), «Нова заявка» з
  домашньої, підказка при порожньому довіднику; дата — datepicker,
  формат дроту `YYYY-MM-DD` (локальні частини, без UTC-зсувів)
- Тести: api unit 108, web unit 50, api-e2e 36, Playwright S-04
  (6 сценаріїв ×3 браузери) + повторний прогін S-02/S-03; смок на реальній
  MySQL — знайшов і виправив реальний баг (PATCH лише з ігнорованими
  полями → помилковий 404; виправлено в tickets і houses)
- Ревʼю (ADR-0010): REQUEST_CHANGES (діапазон 59c407f..67e0aca) —
  1 high (форма створення успадковувала останню переглянуту заявку з
  root-singleton фасаду → `reset()` + e2e-регресія), 2 medium виправлено
  (route.snapshot → toSignal(paramMap); тести ticket-form + empty-submit
  e2e), 1 medium **відхилено з доказом**: DatePipe парсить date-only ISO
  як локальну північ — запропонований 'UTC' зсунув дату на день назад
  (зловила e2e); 1 low (порядок docs-комітів) — дотримано
- Launch-and-look: пройдено очима (підказка → будинок → створення →
  картка #32 → редагування з календарем → картка; мобільний вʼюпорт
  390×844 чистий). Нових іконкових/шрифтових проблем немає
- Fallow: шаблонна складність контейнерів — документовані suppression за
  прецедентом S-03; клон селекторів фасаду (ідіома ADR-0009) — інлайн
  suppression `code-duplication` зареєструвався коректно

## Next 1-2 tasks

- [ ] `/opsx:propose` S-05 «Життєвий цикл і стрічка» (FR-STATUS-01…03,
      FR-FEED-01/02, FR-TICKET-03, FR-DUE-01; таблиця переходів §5.1 —
      окремий ендпоінт переходу, НЕ послаблювати PATCH; `ticket_feed_item`
      append-only)
- [ ] Прод-деплой S-04 за потреби (`git push` тригерить Railway)

## Blockers

- (none). Порт 3000 періодично зайнятий іншим проєктом (`~/Projects/cabinet`)
  — e2e api-e2e при зайнятому порту падає непрозоро, перезапуск допомагає
- ⚠ Робоча гілка — **`service-desk-mini`** (ADR-0012): `main` дзеркалить
  стартер курсу, проєктні коміти в нього не йдуть
